Original source can be downloaded from:
http://code.google.com/p/libarchive/

A list of deleted files is in README.DELETED.

configure hint:
./configure --without-nettle --without-openssl --without-xml2

This source is used in:
	lib/libarchive
	usr.bin/cpio
	usr.bin/tar

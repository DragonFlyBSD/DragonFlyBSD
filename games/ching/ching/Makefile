#	$NetBSD: Makefile,v 1.2 2008/10/30 21:37:55 mrg Exp $

SCRIPTS=	ching.sh
MAN=		ching.6

.if !defined(NO_SHARE)
FILES=		hexagrams macros
FILESDIR=	${SHAREDIR}/games/ching
.endif

.include <bsd.prog.mk>

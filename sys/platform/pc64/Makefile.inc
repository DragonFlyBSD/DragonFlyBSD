# Used by the device build to check for device support
#

DEV_SUPPORT=	acpica bridge crypto disk misc netif \
		pccard powermng raid serial sound smbus \
		virtual/amazon virtual/hyperv virtual/nvmm \
		virtual/virtio virtual/vmware
